/*
41. Magicians: Make a array of magician’s names. Pass the array to a function called show_magicians(), which prints the name of each magician in
the array.
*/

let magicians = [`Fayyaz` , `Ejaz` , `Shahid` , `Junaid` , `Shahab`]

function show_magicians (){
    for(let Names of magicians){
        console.log(Names);
    } 
};

show_magicians()                    // calling function
show_magicians()                    // 2nd time Calling Function
